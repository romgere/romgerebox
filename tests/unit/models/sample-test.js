import Sample from 'romgerebox/utils/models/sample'
import { module, test } from 'qunit'

module('Unit | Model | sample', function () {

  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = new Sample()
    assert.ok(result)
  })
})
